export interface IEvent {
  id: string;
  title: string;
  subtitle: string
  cover_image: string;
  organizer: string;
  reward: number;
  event_flags: string;
  started_date: string;
  finish_date: string;
  sections: string;
  participants: number;
  created: string;
  updated: string;
}